# Review dual — PR #54 (Cierre de todos los issues abiertos)

**Merged:** 2026-08-14 (`0c6a56b`), aprobación explícita del usuario. Issues cerrados: #47, #50, #51, #52, #53.

## Ronda 1

**security-reviewer (remoto — 3 intentos locales muertos por reposo de la máquina): APROBADO**, 0 critical/high. 4 MEDIUM: (1) saneo de single-quotes se traga invocaciones reales con apóstrofes honestos (demostrado empíricamente); (2) los 3 guards fail-open si el `source` del lib falla; (3) fail-open de `jq` legacy en los otros 2 guards; (4) `${UNRESOLVED:-0}` convierte error GraphQL en "0 threads" (legacy). 3 LOW: ancla sin backtick/`(`/`&`; continuación `\` evade el matcher; `sanitize_text` sin DEL/Unicode. Residual: delimitador de issues falsificable (repo PÚBLICO — vector real). Verificado limpio: sanitización #51 sin caminos abiertos, fallback sin perl genuinamente fail-safe, JSON de bloqueo #50 correcto.

**qa-backend: CAMBIOS NECESARIOS** — 2 bloqueantes marginales: (1) falso positivo silencioso sin `perl` (heredoc, mensaje engañoso); (2) `dirname` = dependencia nueva no cubierta por el fail-closed (confirmado por ejecución). Verificó resolución real de los 5 issues y conteos de README contra filesystem.

## Ronda 2 (6 tareas, todas aplicadas — directiva del usuario: nada se difiere)

Orden de saneo invertido + fail-closed integral del source (`${0%/*}` sin dirname) + `jq` check en los 3 guards + aviso stderr en modo degradado + ancla ampliada + join de continuaciones + prefijo `| ` por título + DEL + `.gitignore` (`!.env.example`, `*.p12`, `*.pfx`) + `jq -e` en threads GraphQL.

**Re-reviews (remotos, acotados al delta):** qa-backend **APROBADO** (2/2 bloqueantes resueltos; corrió la suite en copia aislada: 89/89 confirmado independiente). security **APROBADO** con 1 MEDIUM nuevo: el swap de orden **espejó** el bug (número impar de `"` dentro de dos spans single-quoted) — dos pasadas secuenciales no modelan anidamiento.

## Ronda 3 (cambio menor — validado por tests, sin re-review per skill)

Alternancia en una sola pasada (`s/\x27[^\x27]*\x27|"(?:[^"\\]|\\.)*"/ /g`) — el span que abre primero gana, como el parser del shell. Resuelve ambas familias de casos. RED empírico (89/91) → GREEN (91/91).

## Verificación final

Suite adversarial **91/91** por triple vía: dev (con tail de evidencia), QA (entorno aislado remoto) y orchestrator (working tree real, pre-merge). Guards fail-closed integral: sin perl → degradado con aviso (nunca falso negativo); sin jq / sin lib / GraphQL corrupto → bloquean.
