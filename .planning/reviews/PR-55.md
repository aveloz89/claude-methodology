# Review dual — PR #55 (Barrido de follow-ups + plugin)

**Merged:** 2026-08-14 (`b137f7e`), aprobación explícita del usuario. Diff: +1497/−781, 34 commits, 5 lotes + docs + ronda de sugerencias.

## security-reviewer (remoto, rol inyectado) — APROBADO

0 CRITICAL/HIGH/MEDIUM. El archivo de mayor riesgo (`install.sh`, muta el HOME) cumple su invariante destructivo en todos los caminos; **`global/CLAUDE.md` leído entero: cero filtraciones repo-específicas** (el checklist del DESIGN). 3 LOW: prefix-match sin canonicalizar (aceptaba `$REPO/../x` como interno — luego demostrado bug real), `chmod` a través de symlink ajeno, `$1` sin validar en la skill. + robustez de slug (hex vacío) y la adjudicación SKIP-vs-.bak delegada a QA/orchestrator. Limpio: shell injection, secrets, manifests, slug por stdin sin temporales.

## qa-backend (remoto, rol inyectado) — APROBADO

0 blockers. Verificación empírica: suites **108/108 + 19/19** corridas en su sandbox; los 5 escenarios del installer ejecutados (fresca, idempotencia ×2, ajenos intactos, migración legacy, post-migración); tests nuevos validados **por mutación** (paridad detecta hook sin registrar en ambas direcciones; guard de no-contaminación falla ante contaminación inyectada). Los 7 follow-ups del BRIEF resueltos con evidencia por item. 4 sugerencias: off-by-one "13→14 hooks" (marketplace + DESIGN ×3), adjudicar SKIP del installer, imprecisión de redacción en global, higiene del chmod.

## Ronda de sugerencias (5 commits, sin re-review — cambios menores validados por tests)

`c178b0c` canonicalización + chmod condicionado + sugerencia de import (RED demostrado: los symlinks con `..` se borraban) · `85693bc` hex validado en slug (+2 tests) · `ad6f4a0` PR numérico en la skill · `0135532` 14 hooks + DESIGN reconciliado (SKIP bendecido por el orchestrator: más conservador que el contrato) · `62ab2f7` precisión de rulebooks en global.

## Verificación final

Suites **110/110 + 19/19** por triple vía (dev, QA remoto, orchestrator en working tree real). `claude plugin validate --strict` pasa. Migración en vivo verificada: settings materializado, hooks activos vía plugin, dev-loop skills-dir operativo.

## Datos post-merge del log de invocaciones (pendiente #49 cerrado)

`raw_keys` reveló la causa de los `unknown`: payloads con la clave `agent_type` presente pero **null** y forma de evento Stop (`stop_hook_active`, `last_assistant_message`). Además, 35 líneas duplicadas no-adyacentes (mayoría pre-dedupe). Micro-fix futuro: filtrar eventos sin tipo real; no urgente.
