# Review dual — PR #49 (Modernización 2026)

**Merged:** 2026-08-13 (`e0b82af`), aprobación explícita del usuario.

## security-reviewer — APROBADO (ronda 1)

- CRITICAL/HIGH: 0. MEDIUM: 1 (superficie de prompt injection en render de `state.json` — mitigada en ronda de fixes con `sanitize_text()`). LOW: 5 (retención con `xargs` sobre `ls`, umask por defecto, ciclo de vida de snapshots, colisión de slug, truncado no atómico) — 4 aplicadas, 1 diferida (hash de slug → FOLLOWUPS, cambia convención de ARCHITECTURE.md).
- Verificado limpio: command injection (todo stdin vía `jq --arg`), path traversal (prefijo `TS-`, `tr` de `/`), inyección de líneas JSONL (`jq -nc`), secrets (0), aislamiento del sandbox de tests.
- Legacy fuera del diff → issues: #50 (pre-merge-check falla abierto sin perl/jq, `security`+`latent-bug`), #51 (inyección de títulos de issues en contexto de sesión, `security`), #52 (.gitignore mínimo, `security`).
- Detectó además que el diff efectivo excluía el hardening (ya mergeado vía PR #48) → PR body corregido.

## qa-backend — CAMBIOS NECESARIOS (ronda 1) → APROBADO (re-review)

- **Bloqueante:** `session-end-check.sh:19` — `stat -f %m` (con espacio) rompe el fallback GNU en silencio: el marker nunca se escribía en Linux. Reproducido en Docker ubuntu:22.04. Corregido con la forma pegada (`stat -f%m`), verificado 55/55 en ambas plataformas.
- Sugerencias aplicadas: tests de simetría jq-ausente (2 hooks), test de SubagentStop fuera de repo git, y (post-re-review) test adversarial de sanitización de `signals`.
- Diferido: parseo C-quoted de `git status --porcelain` en S2 (heurística informativa, riesgo bajo).
- Observación de datos: log real con entradas duplicadas (doble registro user+project scope) y ~33% `unknown` (subagentes anidados) → corregido en el mismo PR (dedupe por línea idéntica + `raw_keys` de auto-diagnóstico).
- Calidad de suite validada: un hook vacío fallaría los asserts (efectos reales en filesystem, no solo exit codes).

## Ronda de fixes (7 commits) + cierre (1 commit test-only)

`5f8a64b` stat GNU · `c283e4b` tests simetría · `bdc647b` sanitize_text · `c59225f` umask/allowlist/atómico · `90e91c2` docs+FOLLOWUPS · `64810de` dedupe · `ab8e492` raw_keys · `5884492` test signals.

Estado final: suite 56/56 (macOS + ubuntu:22.04), shellcheck limpio, `jq .` válido en ambos settings.
