# Review dual pre-push — feature/user-communication-rules (Fase 2.6)

**Branch:** feature/user-communication-rules · **Base:** dev · **SHA revisado:** HEAD tras las sugerencias aplicadas · **Fecha:** 2026-08-25 · Presupuesto: ~3 min/reviewer (diff +5/−1 en 2 archivos, 100% normativo)

## Ronda 1

### qa-backend — BLOQUEADO

**El runbook prescribía la pregunta de cierre de Fase 0 en prosa**, citada entre comillas para decirla literalmente — exactamente lo que la regla nueva prohíbe. Señaló además que el DoD anti-drift no se había corrido antes de pedir review, y que esta contradicción es justo la clase de caso que ese DoD existe para atrapar. Tenía razón en ambas.

Sugerencias: acotar el alcance de "escribe simple y corto" (no reemplaza los formatos mandatados de artefactos) y aclarar que la regla de opciones no cubre preguntas exploratorias de texto libre.

### security-reviewer — APROBADO, 1 MEDIUM

*"Detalla solo si te lo piden"* habilitaba reportar un CRITICAL en una línea, sin riesgo ni remediación: el blocker técnicamente reportado pero sin la información para evaluarlo. No bloqueante porque el formato del reporte de review ya exige ambos campos y la regla específica de agente gana sobre la general, pero la redacción dependía de que cada agente la leyera bien.

Verificó que la regla de opciones **refuerza** el invariante 3: seleccionar una opción es un acto afirmativo explícito, no inferido del estado de CI. Convierte la aprobación de merge de prosa ambigua —donde un "ok, dale" vago podía pasar por consentimiento— en una selección discreta.

## Ronda 2

**security — APROBADO.** El hueco del blocker queda cerrado y la excepción de artefactos no abre otro: en esos formatos riesgo y remediación son obligatorios por defecto. Dos LOW de precisión, aplicados: el contrato del reporte de review vive en el prompt del agente y no en `rulebooks/`, y la exención de Fase 0 se acotó a las rondas de preguntas, no a su cierre.

**qa-backend — se declaró fuera de scope**, después de haber aceptado el mismo encuadre en la ronda 1 de este PR y en los PRs #59 y #61. Su bloqueante lo verificó el orchestrator: el grep del DoD deja solo la pregunta exploratoria de `ui-ux`, que la cláusula nueva excluye, y los pasos 4 y 5 de la Fase 0 son coherentes — seleccionar "avanzar al diseño" ES la confirmación explícita que el paso 5 exige.

**Segunda aparición del mismo patrón de agente** (la primera quedó registrada en la retro del PR #60): `qa-backend` cambia de criterio de scope entre rondas del mismo PR ante diffs de documentación normativa. Va a follow-ups.

## Cierre

Veredictos limpios, sugerencias aplicadas antes del push.
